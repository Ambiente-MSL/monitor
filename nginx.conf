server {
    listen 80;

    location /api/ {
        proxy_pass http://monitor-backend-jrg0ab:3001/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}